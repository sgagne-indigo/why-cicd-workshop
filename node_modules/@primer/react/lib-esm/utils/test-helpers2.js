import { commonjsGlobal } from '../_virtual/_commonjsHelpers.js';

// JSDOM doesn't mock ResizeObserver
commonjsGlobal.ResizeObserver = jest.fn().mockImplementation(() => {
  return {
    observe: jest.fn(),
    disconnect: jest.fn(),
    unobserve: jest.fn()
  };
});
commonjsGlobal.CSS = {
  escape: jest.fn(),
  supports: jest.fn().mockImplementation(() => {
    return false;
  })
};
